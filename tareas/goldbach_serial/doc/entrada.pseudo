procedure entrada_init(entry) {
    inicia la entrada
} 
procedure run(entry) {
    inicia el arreglo de datos y se 
    prepara para meter los datos en ella
}

procedure entrada_get_files(entry, array) {
    se encargar de revisar la entrada para obtener el archivo
    y poder ignorar los caracteres invalidos
}

procedure entrada_validate(valid) {
    revisa si el numero es valido
}

procedure entrada_print_sumas(actual, valor) {
    imprimir las sumas validadas en los metodos anteriores
}

proceudre entrada_print(array) {
    imprimir la entrada a la consola
}

procedure get_sumas_entrada(actual, valor) {
    obtiene las sumas para poder desplegarlas en la entrada
}

